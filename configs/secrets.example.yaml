# Secrets Configuration Template
# Copy this file to configs/secrets.yaml and fill in your actual values
# DO NOT commit secrets.yaml to Git - it's in .gitignore

roboflow:
  api_key: "your_roboflow_api_key_here"
  workspace: "basketball-dataset"
  project: "hooper_dataset_3006_2235"
  version: 3

# Usage in Python:
# import yaml
# with open('configs/secrets.yaml') as f:
#     secrets = yaml.safe_load(f)
# api_key = secrets['roboflow']['api_key']
